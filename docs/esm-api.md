# ESM API

Placeholder — locked at M5. The exported surface of `lib/crate.js` (`Crate.open`, `list`, `read`, `write`, `remove`, `move`, `mkdir`, `stat`, `history`, `onChange`) becomes a contract that Folio, Slate, Bahi, Mahalla bind to.
