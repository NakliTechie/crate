# Encryption model

Placeholder — populated at M3 alongside `lib/crypto.js` and `lib/manifest.js`. Will document AES-256-GCM key derivation (PBKDF2-SHA256, 600k iterations), recovery-phrase encoding (BIP-39 English), per-file data-key wrapping, IV management, and manifest HMAC signing.
